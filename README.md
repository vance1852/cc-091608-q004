# 跌倒救援编排

该项目记录腕表跌倒候选、家庭联系人策略和救援处置时间线。设备离线与消息提供商重试都可能产生重复消息，因此每个外部事件带有稳定标识。

`src/contracts.ts` 定义跌倒信号、联系人与时间线动作。`fixtures/fall-scenarios.json` 汇集误触取消、无人响应和离线补传三种脱敏场景。

代码面向 Node.js 22 与 TypeScript，安装依赖后通过 `npm test` 进行类型检查。
